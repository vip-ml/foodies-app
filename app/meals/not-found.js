"use client";
export default function Error({ error }) {
    return (
        <main className="not-found">
            <h1>Meal not found!</h1>
            <p>Unfortunately, we could not find the requested page or reesource!</p>
        </main>
    );
}